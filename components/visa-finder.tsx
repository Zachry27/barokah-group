'use client';
import { useMemo, useState } from 'react';
import { ArrowRight, FileCheck2 } from 'lucide-react';
import { visaOptions } from '@/lib/content';
import { whatsappUrl } from '@/lib/site';

export function VisaFinder(){const [country,setCountry]=useState('Saudi');const filtered=useMemo(()=>visaOptions.filter(v=>v.country===country),[country]);return <section className="section" id="visa-finder"><div className="shell"><div className="grid gap-10 lg:grid-cols-[.65fr_1.35fr]"><div><span className="eyebrow text-[#9f7d3d]">Visa finder</span><h2 className="serif mt-3 text-4xl font-semibold">Pilih tujuan, lalu cocokkan kebutuhan.</h2><p className="mt-4 text-sm leading-7 text-slate-500">Kategori ini adalah jalur konsultasi, bukan jaminan penerbitan visa.</p><div className="mt-6 flex flex-wrap gap-2">{['Saudi','Mesir','Dubai'].map(c=><button key={c} onClick={()=>setCountry(c)} className={`rounded-full border px-4 py-2 text-xs font-black ${country===c?'border-[#07131f] bg-[#07131f] text-[#d6b66f]':'border-black/15'}`}>{c}</button>)}</div></div><div className="grid gap-4 md:grid-cols-2">{filtered.map(v=><article key={v.title} className="card p-6"><FileCheck2 size={22} className="text-[#b58d47]"/><h3 className="serif mt-8 text-2xl font-semibold">{v.title}</h3><p className="mt-3 text-sm leading-7 text-slate-500">{v.note}</p><a href={whatsappUrl(`Assalamu'alaikum Barokah Group, saya ingin konsultasi ${v.title}. Tujuan/periode: ...`)} target="_blank" rel="noreferrer" className="mt-6 inline-flex items-center gap-2 text-sm font-black">Tanya persyaratan <ArrowRight size={16}/></a></article>)}</div></div></div></section>}
