'use client';
import Link from 'next/link';
import { Menu, MessageCircle, X } from 'lucide-react';
import { useState } from 'react';
import { whatsappUrl } from '@/lib/site';

const nav=[['Layanan','/#layanan'],['Visa','/visa'],['Bagasi','/bagasi-cairo-jakarta'],['B2B','/b2b'],['Brosur','/promo-brosur'],['Tentang','/tentang']];
export function SiteHeader(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-40 border-b border-white/10 bg-[#07131f]/95 text-white backdrop-blur"><div className="shell flex h-[74px] items-center justify-between"><Link href="/" className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full border border-[#c6a25a]/50 text-xs font-black text-[#d6b66f]">BG</span><span><b className="serif block text-xl leading-none text-[#f2e5cc]">Barokah Group</b><small className="mt-1 block text-[8px] font-black uppercase tracking-[.2em] text-[#c6a25a]">Travel & Land Arrangement</small></span></Link><nav className="hidden items-center gap-6 lg:flex">{nav.map(([label,href])=><Link key={label} href={href} className="text-[11px] font-bold text-white/55 transition hover:text-[#d6b66f]">{label}</Link>)}</nav><div className="hidden items-center gap-2 sm:flex"><a href={whatsappUrl("Assalamu'alaikum Barokah Group, saya ingin konsultasi layanan.")} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full bg-[#c6a25a] px-4 py-2.5 text-xs font-black text-[#07131f]"><MessageCircle size={16}/> WhatsApp</a></div><button aria-label="Buka menu" className="grid size-10 place-items-center lg:hidden" onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t border-white/10 bg-[#07131f] lg:hidden"><nav className="shell grid py-4">{nav.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)} className="border-b border-white/10 py-3 text-sm font-bold text-white/70">{label}</Link>)}<Link href="/kontak" onClick={()=>setOpen(false)} className="py-3 text-sm font-bold text-[#d6b66f]">Kontak</Link></nav></div>}</header>}
